/**
 * ==============================
 * Using functions in JavaScript
 * ==============================
 */
function showMultNumbers( p1, p2 )
{
    return p1 * p2;
}

document.getElementById( 'demo' ).innerHTML = showMultNumbers( 4, 3 );